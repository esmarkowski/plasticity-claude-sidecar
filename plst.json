{
  "modules": [
    {
      "name": "sidecar",
      "binary": "plst-sidecar",
      "build": "go build -ldflags \"-X main.version=$(git describe --tags --always --dirty 2>/dev/null || echo dev)\" -o {{out}} ./cmd/plst-sidecar"
    }
  ]
}
